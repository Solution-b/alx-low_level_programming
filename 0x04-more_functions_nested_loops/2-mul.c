#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @x: the first number to be multiplied
 * @y: the second number to be multipled with the first number
 * Return: the result of the multiples between x & y parameters
 */
int mul(int x, int y)
{
	return (x * y);
}

